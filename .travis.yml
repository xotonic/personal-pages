language: node_js

node_js:
    - "8.10.0"

script:
  - npm run build && npm test

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
    local_dir: public

